# Server
PORT=3001
NODE_ENV=development

# Database (inherited from root .env)
DATABASE_URL=postgresql://doku-seal:password@localhost:5432/doku-seal

# JWT
JWT_SECRET=your-jwt-secret-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
